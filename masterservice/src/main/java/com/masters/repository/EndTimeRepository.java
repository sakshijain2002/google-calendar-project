package com.masters.repository;


import com.masters.entity.EndTime;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EndTimeRepository extends JpaRepository<EndTime,Long> {
}
