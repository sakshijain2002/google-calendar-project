package com.masters.repository;

import com.masters.entity.TimeFormat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TimeFormatRepository extends JpaRepository<TimeFormat,Long> {
}
